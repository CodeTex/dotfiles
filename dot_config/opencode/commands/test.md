---
description: Run or design the narrowest useful pytest checks for the current Python task
agent: python-test-engineer
subtask: true
---

Use the Python testing specialist for this target:

- If `$ARGUMENTS` is empty, infer the most relevant tests from the current changes.
- Use the Python execution skill before running project-local Python tools.
- Prefer the narrowest useful pytest invocation.
- Respect project conventions around pytest-sugar and pytest-cov when relevant.

$ARGUMENTS
