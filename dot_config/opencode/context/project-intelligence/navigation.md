# Project Intelligence Navigation

Purpose: default personal conventions for Python-heavy projects.

Quick routes:
- tooling and coding defaults -> `technical-domain.md`
- long-lived decisions -> `decisions-log.md`

Override rule:
- project-local `.opencode/context/project-intelligence/` overrides this global directory

Update when:
- tooling defaults change
- quality gates change
- a durable workflow decision should be remembered across sessions
